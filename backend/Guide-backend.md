# Sanwad Web App

## Step 1

-   Create signup page
    -   First create signup API named "signup"
    -   User details must be:
        -   Full Name (`fullName`)
        -   Email (`email`)
        -   `dateOfBirth`
        -   `fullAddress` (town, city, state, country)
        -   `profileImage`
-   Create login page
